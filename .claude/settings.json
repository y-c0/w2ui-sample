{
  "permissions": {
    "deny": [
      "Read(claude_log.txt)",
      "Read(**/claude_log.txt)",
      "Grep(claude_log.txt)",
      "Grep(**/claude_log.txt)",
      "Glob(claude_log.txt)",
      "Glob(**/claude_log.txt)"
    ]
  }
}
