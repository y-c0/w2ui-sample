
w2ui 1.5 で店舗一覧を表示する画面を作りたい。
・一覧の項目は
　店舗ID（登録時はサーバ側で採番する）
　店舗名（テキスト）
　都道府県（マスタから選択）
　editボタン
・通常は編集できないが、editボタンを押すと、その行が編集できる
・行を追加して、新しいデータを追加できる
・グリッド外の登録ボタンを押すことで、サーバに編集行のデータ送信する

サンプルを作ってもらうことは可能か？




c:\my\dev\20260817_w2ui1 に、w2ui 1.5 + jQuery + Express の最小サンプルを作成しました。
API 呼び出し(GET/POST)は実際に起動して動作確認済みです。

構成

package.json — Express依存
server.js — 都道府県マスタ、店舗データ(インメモリ)、GET /api/prefectures、GET /api/stores、POST /api/stores(新規行はサーバ側でID採番)
public/index.html — w2ui 1.5 のグリッド + 行追加/登録ボタン
動作の仕組み

グリッドの列(id/name/pref_code/actions)はすべて w2ui の render 関数でカスタム描画しています。w2ui標準のインライン編集ではなく、editingRecid という変数で「今どの行が編集中か」を管理し、その行だけ <input>/<select> を描画、他の行はテキスト表示にする方式です。
「編集」ボタン → startEdit(recid) でその行だけ編集可能に。「完了」ボタンで編集終了(必須項目チェックあり)。同時に編集できるのは1行のみ。
「＋ 行追加」→ id: null の新規レコードを先頭に追加し、即編集モードに。
「登録」→ 全行を POST /api/stores に送信。サーバは id が null の行にだけ新規採番し、既存行は更新。レスポンスの最新データでグリッドを再描画します。
実行方法


npm install
npm start
その後ブラウザで http://localhost:3000 を開いてください。

なお動作確認時に curl で日本語データを送信した際、ターミナルの文字コード起因で応答が文字化けしましたが({name: "TestStore"} のようなASCIIでは正常)、これはターミナル側の問題で、実際のブラウザの fetch() は UTF-8 で送るため問題ありません。ID自動採番などのロジック自体は正しく動作しています。

質問や要望(例: 削除ボタン追加、バリデーション強化、ページング対応など)があれば教えてください。

＝＝＝＝＝＝＝＝＝＝
追加要件案。
・店舗一覧から、お気に入り店舗を選べる。
・店舗一覧の初期表示時に、お気に入りに設定した店舗だけが表示されるようにする。　それ以外の店舗は、グリッドのリロードか、グリッドの検索機能から検索することで表示される。

このための実装として、
・店舗一覧に「お気に入り」のチェックボックスを追加
・画面側のCookieに、「お気に入り店舗のリスト」を持ち、画面表示時にチェックボックスに反映する
・チェックボックスを編集すると、Cookieに保存される
とするのはどうか？
店舗数が数百件になり、ページングが発生する状況などを考えると、現実的ではないように思う。この案は適切と考えるか？または、別の案はあるか？
まだ実装は行わなくてよい。検討してほしい。



結論から言うと、Cookie案は数百件規模＋ページング前提だと適切ではないと思います。理由は以下の通りです。

Cookie案の問題点

サイズ/送信コストの問題: Cookieは通常4KB程度の上限があり、しかも(pathを絞らない限り)そのドメインへの全リクエスト——グリッドのAPI呼び出しだけでなく画像やJS/CSSの静的リソース取得にまで——毎回付与されます。お気に入りが数十〜百件規模になると無駄な通信コストが無視できなくなります。
「初期表示はお気に入りのみ」とページングの根本的な矛盾: ページングが発生する規模ということは、初期表示で全店舗をサーバから取得してクライアント側でCookieのIDリストによりフィルタする、という方式は成立しません(全件取得自体をやめたいはずなので)。だとすると「お気に入りだけ表示」はサーバ側の検索条件として実装する必要があり、Cookie(クライアントだけが知っている情報)ではサーバがそのフィルタ条件を知りようがありません。
マルチデバイス/マルチブラウザで同期しない: 会社のPCとノートPCで別々のお気に入りになる、ブラウザのCookie削除で消える、といった業務システムとしては望ましくない挙動になります。
代案: お気に入りをサーバ側で永続化する

user_favorites(または store.is_favorite)のようなテーブルをDB側に持ち、「お気に入り店舗一覧」はサーバ側のクエリで絞り込む。
w2uiのグリッドは実は最初から「リモートデータソース(url指定)＋サーバ側ページング/検索」という仕組みを持っているので、初期表示時に favorite=true という検索条件付きでサーバへリクエストし、サーバ側で WHERE id IN (SELECT store_id FROM user_favorites WHERE user_id=?) のように絞り込んだ結果だけを返す、という形が自然です。今回のサンプルは全件をクライアントにロードする方式でしたが、数百件＋ページングを見据えるなら、この機会にリモートグリッド方式へ移行するのが筋が良いと思います。
「リロード」や「検索」で全件・条件検索に切り替えるのも、既存の検索条件を変えるだけなので実装として素直です。


方針が固まりました。まとめると:

設計方針

お気に入りはユーザー単位でDBに保存(例: user_favorites(user_id, store_id))。ただし現状のサンプルには認証機構がないため、実装に進む際は「ユーザーIDをどう取得するか」(セッション/JWT/簡易ログインなど)を先に決める必要があります。
グリッド初期表示は、w2uiのリモートデータソース機能を使い、サーバへ favorite_only=true 相当の検索条件を送って絞り込み結果のみ取得。「リロード」や検索欄のクリアで条件を外せば全件検索に切り替わる、という自然な流れになります。
お気に入りチェックボックスは、行の編集(店舗名・都道府県)とは独立した操作として、クリックした瞬間に POST/DELETE /api/favorites のような専用APIを叩いて即保存。既存の「編集→登録」フローとは分離するので、他の項目を編集中でもお気に入りだけは自由にON/OFFできます。
残っている検討事項(実装前に必要)

ユーザー識別の方法(認証の有無・方式)
グリッドを「全件クライアントロード」から「リモートデータソース(サーバ側ページング/検索)」に切り替える設計変更(現サンプルからの構成変更が必要)
このあたりの方向性で良ければ、認証方式が固まり次第、実装に着手します。



＝＝＝＝＝＝＝＝＝＝

・方針について
ユーザー認証は設けるので、ユーザIDを取ることは何らかの形でできるはず。
サーバ側はSpringBootを使っており、ユーザ／パスワードでの認証はある。

グリッドを「全件クライアントロード」から「リモートデータソース(サーバ側ページング/検索)」に切り替え
　→リモートデータソースにする。既存のアプリの実装もそうなっている。

・依頼
実装する前に一度gitコミットしてほしい。リモートリポジトリは無し、一旦ローカルのみでよい。






519e527 としてコミットしました(検証用の scratch_*.js / shot*.png は未追跡のままローカルに残しています)。

実装したもの

お気に入り チェックボックス列を追加。クリック即座に POST/DELETE /api/favorites/:storeId で保存(店舗名・都道府県の編集フローとは独立)。
グリッドを w2ui のリモートデータソース(url: '/api/stores/search')に切り替え、GET /api/stores/search?request=<JSON> でサーバ側フィルタ/ページング/検索を行う構成に変更(全件クライアントロードは廃止)。
初期表示は favorite_only: true でお気に入り店舗のみをサーバ側で絞り込み。チェックを外すと全12件がページング付きで表示される。
実装中に分かった重要な点

w2ui 1.5.3の実際の通信仕様は、事前の想定(POST + JSON body)と異なり、GET + request=<URLエンコードJSON> クエリパラメータでした。実ソースを確認して修正しています。Spring Boot側で実装する際もこの形式(GETリクエスト、requestパラメータにJSON文字列)に合わせる必要があります。
ブラウザでの実動作をPlaywrightで確認済み: お気に入りのみの初期表示、行編集の1行制限、全件表示時のページング読み込み、いずれも正常動作。ただし全件表示時は「1-12 of 12」のようにスクロールに応じて追加読み込みする挙動(w2uiの pull-more)になっており、ページ番号ボタン式ではありません。実アプリのページングUIがボタン式であれば、そこは調整が必要です。
未対応・要検討事項

ユーザーIDはモックとして固定値(user1)をヘッダで送信しているだけです。Spring Boot側の実認証と繋ぎ込む際は、このヘッダ送信部分を実際のセッション/トークンベースの認証に置き換える必要があります。



＝＝＝＝＝＝＝＝＝＝



・カラム名を以下の通り変更してほしい。
　店舗　nm_cvs_store
　店舗ID　id_cvs_store
　都道府県ID cd_region
　都道府県　nm_region

・都道府県と同様に、プルダウンから選択する形で、以下項目を追加してほしい。
　立地
　　立地　nm_cvs_location
　　立地ID　cd_cvs_location　非表示
　チェーン
　　チェーン　nm_cvs_chain
　　チェーンID　cd_cvs_chain　非表示



・一度コミットしてください
・行追加をしたあとで検索を押すと、編集中の行がない　かつ　編集完了を待っている状態になり、矛盾してしまう。
　編集中　または　行追加した状態で、検索をしようとした場合
　編集中状態をリセットします。よろしいですか？
　とメッセージを出し、
　　OK→編集中状態をリセットして検索
　　キャンセル→編集を継続し、検索をしない
としたい


この店舗一覧は、店舗についての調査記録を投稿するアプリの中で
ポップアップ画面として使う想定をしている。

この中から１件選択して、投稿情報一覧に戻る　という挙動にしたい。

・グリッドの下に、OK・キャンセル　ボタンを追加してほしい。

・OKボタン
　選択中の１行を取得する。
　取得後の処理や画面遷移は不要。こちらで組込時に対応する。
　編集中の行がある場合、編集完了するよう警告を出す。
　編集を破棄する選択肢は無しとする。

・キャンセルボタン
　店舗の選択をせずにポップアップを閉じる。
　取得後の処理や画面遷移は不要。こちらで組込時に対応する。
　編集中の行がある場合、編集完了するよう警告を出す。
　　警告と同時に、編集を続ける・編集を破棄する　ボタンを表示する。


この設計で問題がないか、いったん検討してほしい。


コミットは、ブランチをきったうえでやってほしい。


feature/popup-select-ok-cancel ブランチを作成し、c9cd289 としてコミットしました(master は変更していません)。

実装内容

行クリックで1件選択(w2ui標準の行選択、単一選択)
OK: 未保存の変更(編集中/完了済み未登録)があれば警告してブロック。なければ選択行を window.onStoreSelect(record) に渡す
キャンセル: 未保存の変更があれば「続ける/破棄する」の確認ダイアログ。最終的に window.onStoreCancel() を呼ぶ
画面遷移や後処理は行わず、上記2つのグローバル関数を呼び出すところまでを実装(組込時に上書きして使う想定)
Playwrightで6パターン(未選択OK、選択後OK、編集中OK/キャンセル、完了済み未登録でのOK/キャンセル)すべて想定通りの動作を確認し、既存機能(登録・検索ガード・ソートガード)のリグレッションもないことを確認済みです。

master にマージする際は教えてください。

＝＝
ポップアップを開いた場合についても追加。
すでに店舗を指定した状態で画面を開いたら、その店舗を選択した状態で画面を開くようにしたい。
店舗IDを画面の外側から取得するデータのため、店舗IDを指定した体裁で画面を表示する方法が明確になっていればよい。
店舗IDを指定された場合、検索条件からお気に入りを除外する。（お気に入り以外の店舗を選んでいる可能性があるため）


23dcdd2 として feature/popup-select-ok-cancel ブランチにコミットしました。

実装内容

?store_id=2 のようにURLクエリパラメータを付けて開くと、「お気に入りのみ表示」を自動的に外し、その店舗だけをサーバー側で絞り込んで確実に取得した上で行を選択状態(青ハイライト)にします
選択後は絞り込み条件を解除し、以降の検索・リロード・ソートは通常通り全件を対象にします
実装上の注意点(今後のため記録): w2uiの onLoad のような設定オプション形式のイベントハンドラは実は before フェーズでしか呼ばれない仕様でした(ソースコードで確認)。ロード完了(after)を検知するには grid.on('load:after', ...) という別のAPIを使う必要があり、最初の実装はこれに気づかず動作しませんでした。Playwrightで実際に選択状態・ハイライトを確認しながら修正しています。


＝＝
?store_id=2 のようにURLクエリパラメータを付けて開く場合について訂正。
お気に入りを指定せずに検索する
検索結果内に、対象店舗があれば選択状態にする
としたい。
ページングによりまだ取得していないデータの中に選択した店舗がある可能性がある。
その場合、店舗を未選択の状態としてよい。


・stpre_id=6以上の場合に、正しく動いていないように見えるが、内部でページングをしている？
・画面の表示時に、api/stores/search を３回実行しているのもその影響か？
ページングの検証のためにあえてそうしているなら、問題はない。
この3回のAPI呼び出しは、あえて仕込んだものではなく、w2uiグリッドの「pull-more(自動追加読み込み)」という仕様の副作用です。以前(お気に入り解除時の全件表示のとき)にも一度触れましたが、今回の store_id 機能でも同じ影響が出ています。

何が起きているか

グリッドの表示エリアの高さ(420px)は、1ページ分(5件)より多くの行を表示できる広さがあります。
w2uiは「表示エリアがまだ埋まっていない」と判断すると、ユーザー操作なしに自動で次のページを追加取得し続けます(offset 0→5→10 で計3回、5件+5件+2件=12件で埋まって停止)。
結果として、ページを開いた直後にほぼ全件(12件)が画面に表示されてしまい、「1ページ目だけがロードされている」状態を実質視認できません。
store_id=6以上で選択されないように見える件について

これはバグではなく、直前にご指定いただいた仕様通りの動作です。私の実装は「load:afterイベントが最初に発火した時点」＝サーバから最初に返ってきたoffset0のページ(id 1〜5)だけを対象に選択判定をしており、以降のpull-more分では選択処理を再実行しません。そのため:

store_id=1〜5 → 最初のページに含まれる → 選択される
store_id=6以上 → 最初のページには含まれない → 仕様通り未選択のまま
ただしpull-moreによって残りの行もすぐ画面に出てしまうため、「もう見えているのに選択されていない」という違和感のある見た目になっています。仕様上は正しいのですが、体験としては分かりにくいかもしれません。

これは元々ページング動作の検証用に意図していたものではないので、対応方針を選んでいただけますか？

AskUserQuestion
OUT
Your questions have been answered: "pull-more(自動追加読み込み)の振る舞いについて、どう対応しますか？"="今回は何もしない(仕様通りの動作なので問題なし)". You can now continue with these answers in mind.
了解しました。pull-moreの挙動は今回そのままにします。



店舗は今後増えるので、あとで組み込むときに、５件ずつ読んでいる設定は見直すことにする。どこを治せばよい？
public/app.js の w2grid({...}) 設定内、limit: 5 の1行です。

Grep "limit: 5" (in c:\my\dev\20260817_w2ui1\public\app.js)
1 line of output
public/app.js:293 の limit: 5 が1ページあたりの取得件数(サーバへの limit パラメータ)です。ここが唯一の設定箇所で、店舗数の増加に応じてこの値を調整すれば挙動が変わります。

参考までに、この値は他にも影響します。

グリッドの高さ(#grid { height: 420px; }、public/index.html:17)との兼ね合いで、limitより表示エリアが広いと、前回お話しした「pull-more(自動追加読み込み)」が発生し、limit件を超えて自動取得され続けます。店舗数が増えた際に画面を見やすくページ区切りしたい場合は、limitだけでなく高さやpull-more自体の扱いもあわせて見直すことになります。
store_id 指定時の初期選択(前回実装分)は「1ページ目(＝limit件)に対象店舗が含まれていなければ未選択のまま」という仕様なので、limitを変えると「見つかる/見つからない」の境界も変わります。
「あとで見直す」というメモとして、この3点(limit, グリッド高さ, pull-more方針)をセットで扱うと決め忘れがないと思います。




＝＝
検索条件無しで検索し、グリッドをスクロールすると、
グリッドの一番下で、待機中の回るアイコンが出た状態になる
クリックすると
Your remote data source record count has changed, reloading from the first record
と出る。これを解消できるか？

原因を特定しました。全件読み込み後も画面下部の「もっと読む」スピナー要素がDOM上に残ったままクリック可能になっており、
それを押すと存在しないオフセットへの追加リクエストが発生し、
w2ui内部の件数チェックが不整合を検出してこのメッセージを出す、というw2ui側の挙動でした。
load:after のタイミングで、読み込みが完了していたらこの要素を隠す形で対処します。



＝＝